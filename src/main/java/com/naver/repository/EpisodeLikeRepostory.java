package com.naver.repository;

import com.naver.entity.EpisodeLike;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EpisodeLikeRepostory extends JpaRepository<EpisodeLike, Long> {
}
